spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/labo_final_jdr
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

#  security:
#    user:
#      name: pol
#      password: truc
#      roles:
#        -ADMIN

springdoc:
  swagger-ui:
    path: /swagger